#!/usr/bin/env python2

import time

stuff = range(0, 10000000)
key = 'arm'
start = time.time()
for i in stuff:
    key != 'amd64' and key != 'all'
print(time.time() - start)

start = time.time()
for i in stuff:
    key not in ('amd64', 'all')
print(time.time() - start)
